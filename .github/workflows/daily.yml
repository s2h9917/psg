name: daily-pick

on:
  schedule:
    - cron: '40 23 * * *'    # 08:40 KST(전일 23:40 UTC) → 시초가(open)
    - cron: '0 6 * * *'      # 15:00 KST(당일 06:00 UTC) → 종가(close)
  workflow_dispatch:
    inputs:
      session:
        description: '세션 선택 (open=시초가 / close=종가)'
        default: 'close'
        type: choice
        options: [open, close]

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install deps
        run: pip install -r requirements.txt
      - name: Generate & send picks
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          MARKETS: 'KOSPI,KOSDAQ'
          PER_MARKET: '3'
          # 오전 스케줄(23:40 UTC)=시초가, 그 외=종가. 수동 실행 시 입력값 우선.
          SESSION: ${{ github.event.inputs.session || (github.event.schedule == '40 23 * * *' && 'open' || 'close') }}
        run: python notify.py
      - name: Commit history
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add recommendation_history.csv || true
          git commit -m "auto: ${SESSION:-picks} $(date +%F)" || echo "no changes"
          git push || echo "push skipped"
